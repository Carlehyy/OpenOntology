"""哨兵 CEP（复杂事件处理）支持层 — 叶子子包。

职责：实例级事件日志（时间事实的持久化底座）与受限时间算子，以及
（后续里程碑）水位驱动的模式匹配器。本包只能被 sentinels 域内的
cdc/engine/evaluator 单向导入，禁止反向依赖这些模块或 dynamic_service，
否则会制造新的依赖环并被架构守卫拒绝。

设计决策（架构评审 2026-09-06 收敛）：
- 事件日志是唯一时间事实源；CDC outbox 只负责触发与聚合，不携带值。
- 事件时间为数据库事务内捕获时刻（UTC），行按单调递增大整数主键定序。
- 来源标记区分真实业务变更（organic）与发布切换期间的投影重建
  （release_activation）：后者不参与时间算子与模式推进，避免发布洪峰
  被误判为全量变更。
"""
